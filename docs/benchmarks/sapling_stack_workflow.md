# Sapling stacking workflow report

This report is generated by `mix micelio.sapling.stack_workflow`.

Sessions simulated: 3
Started at: 2026-01-21T00:17:45.146654Z

Tools requested: `git, sapling`
Available tools: `git`
Missing tools: `sapling`

## Workflow

- Initialize a repo per tool
- Create a base commit
- Create 3 agent session commits (stacked branches for Git)
- Capture stack/log views

## Results

### git
Repo path: `/tmp/sapling_stack_workflow_git_2946`
Status: ok

init_repo: `git init` (status 0)
Output:
```
Initialized empty Git repository in /tmp/sapling_stack_workflow_git_2946/.git/
```

configure_identity: `git config user.name Micelio Agent` (status 0)
Output:
```
(no output)
```

stage_base: `git add -A` (status 0)
Output:
```
(no output)
```

commit_base: `git commit -m base: initialize repo` (status 0)
Output:
```
[master (root-commit) 17fc857] base: initialize repo
 1 file changed, 3 insertions(+)
 create mode 100644 README.md
```

checkout_branch: `git checkout -b agent/stack-1` (status 0)
Output:
```
(no output)
```

stage_session: `git add -A` (status 0)
Output:
```
(no output)
```

commit_session: `git commit -m agent/session-1: update` (status 0)
Output:
```
[agent/stack-1 8c2b6cc] agent/session-1: update
 1 file changed, 3 insertions(+)
 create mode 100644 notes/session_1.md
```

checkout_branch: `git checkout -b agent/stack-2` (status 0)
Output:
```
(no output)
```

stage_session: `git add -A` (status 0)
Output:
```
(no output)
```

commit_session: `git commit -m agent/session-2: update` (status 0)
Output:
```
[agent/stack-2 0e1b673] agent/session-2: update
 1 file changed, 3 insertions(+)
 create mode 100644 notes/session_2.md
```

checkout_branch: `git checkout -b agent/stack-3` (status 0)
Output:
```
(no output)
```

stage_session: `git add -A` (status 0)
Output:
```
(no output)
```

commit_session: `git commit -m agent/session-3: update` (status 0)
Output:
```
[agent/stack-3 583828b] agent/session-3: update
 1 file changed, 3 insertions(+)
 create mode 100644 notes/session_3.md
```

stack_view: `git log --graph --oneline --decorate --all -n 20` (status 0)
Output:
```
* 583828b (HEAD -> agent/stack-3) agent/session-3: update
* 0e1b673 (agent/stack-2) agent/session-2: update
* 8c2b6cc (agent/stack-1) agent/session-1: update
* 17fc857 (master) base: initialize repo
```

log_view: `git show-branch --more=5` (status 0)
Output:
```
! [agent/stack-1] agent/session-1: update
 ! [agent/stack-2] agent/session-2: update
  * [agent/stack-3] agent/session-3: update
   ! [master] base: initialize repo
----
  *  [agent/stack-3] agent/session-3: update
 +*  [agent/stack-2] agent/session-2: update
++*  [agent/stack-1] agent/session-1: update
++*+ [master] base: initialize repo
```


