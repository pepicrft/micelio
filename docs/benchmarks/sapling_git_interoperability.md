# Sapling Git interoperability report

This report is generated by `mix micelio.sapling.interoperability`.

Started at: 2026-01-21T00:29:56Z

Tools requested: `git, sapling`
Available tools: `git`
Missing tools: `sapling`

## Tool versions

- git: git version 2.43.0

## Summary

Git repo opened with Sapling: blocked (status ok)
Sapling git-backed repo opened with Git: blocked (status skipped)

## Scenarios

- Git repo opened with Sapling (Sapling reads Git metadata)
- Sapling git-backed repo opened with Git (Git reads Sapling commits)

## Results

### Git repo opened with Sapling
Repo path: `/tmp/sapling_interop_git_manual_Wsixmy`
Status: ok
Compatibility: blocked

git_init: `git init` (status 0)
Output:
```
hint: Using 'master' as the name for the initial branch. This default branch name
hint: is subject to change. To configure the initial branch name to use in all
hint: of your new repositories, which will suppress this warning, call:
hint: 
hint: 	git config --global init.defaultBranch <name>
hint: 
hint: Names commonly chosen instead of 'master' are 'main', 'trunk' and
hint: 'development'. The just-created branch can be renamed via this command:
hint: 
hint: 	git branch -m <name>
Initialized empty Git repository in /tmp/sapling_interop_git_manual_Wsixmy/.git/
```

git_config: `git config user.name Micelio Agent` (status 0)
Output:
```
(no output)
```

git_add: `git add -A` (status 0)
Output:
```
(no output)
```

git_commit: `git commit -m base: git repo` (status 0)
Output:
```
[master (root-commit) 91fe53a] base: git repo
 1 file changed, 3 insertions(+)
 create mode 100644 README.md
```

sapling_status: `sl status` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

sapling_log: `sl log -l 5` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

### Sapling git-backed repo opened with Git
Repo path: `/tmp/sapling_interop_sapling_manual_missing`
Status: skipped
Compatibility: blocked

sapling_init_git: `sl init --git` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

sapling_config: `sl config ui.username Micelio Agent <agent@micelio.dev>` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

sapling_add: `sl add .` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

sapling_commit: `sl commit -m base: sapling repo` (status skipped)
Output:
```
skipped (missing tool: sapling)
```

git_status: `git status --short` (status skipped)
Output:
```
skipped (sapling repo not ready)
```

git_log: `git log -n 5 --oneline` (status skipped)
Output:
```
skipped (sapling repo not ready)
```
