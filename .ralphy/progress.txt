# Ralphy Progress Log

- Added profile bio/social link fields, settings form, public display with icons, plus validations and tests.
- Added public profile activity feed with landed sessions, stars, and project creation entries, plus pagination, styling, and tests.
- Added popular projects section to homepage with pagination, star counts, and tests.
- Added circular favicon.ico asset, linked it in the layout, and covered it with a homepage layout test.

- Added audit logging for project operations with audit log schema, persistence, and tests.

- Added TOTP-based two-factor authentication with account setup, login challenge, and tests (plus UI and Playwright verification).

- Centralized branch protection checks, added normalization tests, and marked the PRD item complete.

- Added secret scanning during session landings to block credential leaks, with GRPC coverage test.

- Added gRPC branch protection test coverage for non-main targets and aligned project access token schema/changesets to clear lint warnings.

- Consolidated legal pages into a single /legal page with redirects, updated footer link, and added controller tests.

- Added authenticated API rate limiting with per-user buckets, config defaults, and updated tests.

- Added abuse detection and mitigation on API rate limiting with blocklist enforcement and plug tests.
- Added OAuth token endpoint for mobile refresh flow, fixed Boruta client adapter return types, and covered refresh handling with tests.
- Added Android project browsing and detail views with in-memory repository data, plus navigation, tests, and lint/test runs.
- Added Android project browsing and detail screens with in-memory data, navigation, and tests.
- Added mobile project API endpoints with pagination/sync metadata, visibility filtering, and tests.

- Added provisioning request normalization for agent VM plans with volume mount mapping, plus types and tests.
- Evaluated cloud platforms for agent VM provisioning with a curated comparison module, API accessors, and tests.
- Completed the cloud-agnostic VM management abstraction by covering the provider module resolution in AgentInfra tests and marking the PRD item done.

- Improved remote execution enqueue error handling to mark failed tasks when the supervisor is unavailable, added tests, and marked the PRD item complete.

- Added LLM model selection to the new project form with organization-specific options and LiveView coverage.
- Enforced sandbox network isolation by stripping network config when policy is none, added test coverage, and marked the PRD item complete.
- Marked the agent resource quota and billing milestone complete after verifying implementation and coverage.

- Added repository validation during imports (git fsck), surfaced a validation stage, and covered it with tests.

- Marked agent OG images with LLM summaries as complete in PRD after confirming implementation and coverage.

- Refined agent progress OG image layout with stat cards, added snapshot fallback test, and marked the PRD item complete.

- Added no-cache headers for social crawlers in OG cache buster plug, with tests, and marked the PRD item complete.
- Added OG image templates for commits, pull requests, and agent sessions, wired session pages to stats-based OG images, and added coverage.

- Marked the Prompt Request System task complete in the PRD after confirming implementation and coverage.

- Added coverage threshold tests for validation checks and marked the ephemeral validation environments task complete.


- Added a live view test for human-origin prompt requests to confirm transparency fields render without model metadata, and marked the AI contribution transparency task complete in the PRD.

- Added decay coverage for reputation scoring and marked the trust scoring PRD item complete.

- Added error tracking feature flag config for optional external Sentry forwarding, with tests.


- Marked error schema/storage task complete in PRD after confirming schema + retention helpers, and added retention config tests.
- Marked error capture pipeline complete in the PRD after confirming capture integrations and tests.

- Added a LiveView error boundary component with sanitized context capture, fallback UI, and applied it to agent progress, plus tests.


- Marked agent/background job error capture task complete in PRD after confirming Oban/agent reporters and tests.
- Built the admin error dashboard LiveView with overview metrics, filters, detail view actions, copy stacktrace hook, and tests.

- Added webhook and Slack notifier coverage for error notifications, and marked the PRD item complete.

- Marked error capture rate limiting/retention policies complete in PRD after confirming implementation and tests.

- Completed the agent session event schema task with validation coverage and marked it done in the PRD.

- Added coverage for session event payload fallback to output and marked the event capture layer task complete.
- Added SSE session event stream coverage for the type alias filter and marked the event streaming API task complete.

- Added data-role hooks for session event details, covered the payload details rendering in LiveView tests, and marked the event viewer UI task complete.

- Marked session event rich rendering complete in the PRD after confirming inline images, progress bars, and collapsible output rendering plus coverage.

- Added benchmark tool availability/version reporting, generated a Sapling-vs-Git benchmark report (Git-only due to missing Sapling), and marked the PRD item complete.
- Marked AI token pool schema/API task complete after confirming schema, context, controller, and test coverage.
- Marked the Sapling stacking workflow simulation task complete after verifying the report generator and tests.

- Added API token contribution endpoint for AI token deposits with auth/error handling and tests, then marked the PRD item complete.
- Added tool version reporting to Sapling interoperability runs with test coverage, and marked the PRD task complete.

- Added coverage to ensure non-admin members see the task budget note instead of the allocation form, and marked the task budget UI item complete in the PRD.
- Integrated AI token budget checks into agent runner provisioning with coverage.

- Added coverage for Sapling integration design open questions rendering and marked the integration layer task complete in the PRD.

- Added empty-state coverage for the admin usage dashboard and marked the PRD item complete.
- Added a Firecracker vs containers benchmark plan with metrics/workload matrix, covered by doc test, and marked the PRD item complete.

- Added a Nomad + Firecracker prototype runbook with sample job spec and config artifacts, plus coverage to verify the prototype assets.

- Added a Packer-based Firecracker image builder PoC with autoinstall config, bootstrap script, and test coverage, then marked the PRD item complete.

- Added original prompt display in prompt request show UI, covered it with LiveView assertions, and marked the PRD item complete.
- Expanded the session manager API design doc with state transitions, idempotency, and observability details, added doc coverage tests, and marked the PRD task complete.

- Added Fly.io fallback provisioning for validation environments with provider failover logic and tests.
- Added a GitHub Actions vs ephemeral VM cost/performance benchmark plan doc with metrics, cost model, and decision criteria, plus doc coverage tests.
- Added mic integration design doc for ephemeral sessions with scope, lifecycle, and security details, plus doc coverage, and marked the PRD item complete.

- Forced API prompt request submissions to run validation synchronously so accepted requests create sessions immediately, updated controller tests, and marked the PRD item complete.
- Added quality threshold feedback to prompt request validation, recorded threshold metadata, covered new behavior with tests, and marked the quality gate pipeline task complete.

- Added admin prompt registry and prompt templates UI, wired template selection/curation, covered with LiveView tests, and marked the PRD task complete.

- Added structured validation feedback rendering for prompt requests with quality scores, failure details, suggested fixes, and tests.

- Added confidence-based auto-accept gating for prompt requests with low-confidence pending behavior, plus tests.

- Marked S3Config schema/migration task complete in PRD after confirming implementation and tests.- Marked credential encryption with Cloak complete in the PRD after confirming vault config, encrypted types, rotation support, and tests.

- Added step-aware S3 credential validation results with test coverage and marked the PRD task complete.

- Added telemetry coverage for user S3 fallback behavior and marked the storage fallback PRD task complete.
- Added a LiveView test for toggling S3 secret key visibility and marked the storage settings UI task complete.

- Marked the storage security/IAM documentation task complete in the PRD after confirming docs, rate limiting, and audit logging coverage.
